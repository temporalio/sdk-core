# WASM Hello Workflow

Build the component with:

```bash
cargo component build --release --target wasm32-unknown-unknown
```

The resulting component artifact is written under `target/wasm32-unknown-unknown/release/`.
